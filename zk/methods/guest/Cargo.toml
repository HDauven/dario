[package]
name = "dash-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "3.0.5", default-features = false, features = ["std"] }
dash_core = { path = "../../../dash_core" }
